const INITIAL_STATE = { common_data: 'good ole common grog!' };

const CommonReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default CommonReducer;
